CLASS DailyTasks
    ATTRIBUTE: LinkedHashMap<taskId, description>
    ATTRIBUTE: taskCounter = 0

    METHOD addTask(description)
        taskCounter++
        tasks[taskCounter] = description

    METHOD removeTask(taskId)
        DELETE tasks[taskId]

    METHOD viewTasks()
        FOR each task in tasks (in insertion order)
            DISPLAY taskId, description
